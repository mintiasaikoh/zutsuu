# 気圧アラート (zutsuu)

気圧変化による体調不良を事前に通知し、薬を飲むタイミングを知らせるツール。

## 概要

- 1時間ごとに気圧予報をチェック
- 気圧の急変が始まる**約1時間前**にLINEで通知
- 上昇・下降どちらも検知（気圧の「変化」が体調に影響するため）

## 通知ロジック

```
毎時0分に実行
    ↓
今後12時間の毎時気圧変化を計算
    ↓
1時間あたり ±2hPa 以上の変化（＝急変）を探す
    ↓
急変開始の約1時間前なら通知
```

### なぜこのタイミング？

- 気圧変化による体調不良には**薬（五苓散など）が有効**
- 薬は変化が始まる**30〜60分前に飲むのが効果的**
- そのため、急変開始の1時間前に通知する

## 設定値

| 項目 | 値 | 説明 |
|------|-----|------|
| 急変閾値 | ±2 hPa/時 | これ以上の変化で「急変」と判定 |
| 通知タイミング | 1時間前 | 急変開始の何時間前に通知するか |
| 地域 | 練馬区 | 緯度35.74, 経度139.65 |

## 通知メッセージ例

```
💊 薬を飲むタイミングです

📉 14:00頃から気圧が低下します

1018 hPa → 1012 hPa
（3時間で -6.0 hPa）

今のうちに対策しておきましょう。
```

## 技術スタック

- **実行環境**: GitHub Actions (cron: 毎時0分)
- **言語**: TypeScript
- **気圧データ**: Open-Meteo API (無料・キー不要)
- **通知**: LINE Notify

## ファイル構成

```
zutsuu/
├── .github/workflows/check.yml  # 定期実行設定
├── src/index.ts                 # メインロジック
├── package.json
├── tsconfig.json
├── SPEC.md                      # この仕様書
└── .gitignore
```

## セットアップ

### 1. LINE Notifyトークン取得

1. https://notify-bot.line.me/ にログイン
2. 「トークンを発行する」をクリック
3. トークン名を入力（例: 気圧アラート）
4. 通知先: 「1:1でLINE Notifyから通知を受け取る」
5. トークンをコピー

### 2. GitHubリポジトリ作成

```bash
git init
git add .
git commit -m "Initial commit"
gh repo create zutsuu --private --source=. --push
```

### 3. Secretsにトークン登録

```bash
gh secret set LINE_TOKEN
# プロンプトでトークンを貼り付け
```

### 4. 動作確認

```bash
gh workflow run check.yml
gh run list --workflow=check.yml
```

## 気象病について

### メカニズム

1. 内耳が気圧センサーとして機能
2. 気圧変化を検知すると交感神経が過剰に興奮
3. 自律神経のバランスが崩れ、症状が発現

### ポイント

- **低気圧だけでなく、気圧の「変化」が問題**
- 上昇時も下降時も体調不良は起こる
- 1時間あたり±2hPa以上の変化が要注意

### 対策

- 事前に薬を服用（五苓散など）
- 耳周りのマッサージ・温め
- 規則正しい生活、十分な睡眠

## 今後の拡張案

- [ ] 緯度経度を環境変数化（複数地域対応）
- [ ] 閾値のカスタマイズ
- [ ] 通知履歴の記録（重複通知防止）
- [ ] 朝の定時予報（今日の気圧変化サマリー）
