# 気圧アラート (zutsuu)

気圧変化による体調不良を事前に通知し、薬を飲むタイミングを知らせるツール。

## 概要

- 1時間ごとに気象データを取得し、複合リスクスコアを算出
- リスク急上昇の**約1〜2時間前**にLINEで通知
- 朝（8:30〜10:00）には総合予報を送信
- 夜間（22:00〜8:30）は通知オフ

## 複合リスクスコア

気圧だけでなく、複数の気象要素を組み合わせてリスクを評価する。

### スコア内訳（最大18pt）

| 要素 | 条件 | スコア |
|------|------|--------|
| 気圧変化1h | ±2hPa以上 | +1〜3pt |
| 気圧変化3h | ±4hPa以上 | +1〜3pt |
| 気圧変化6h | ±6hPa以上 | +1〜2pt |
| 絶対気圧 | 1005hPa以下 | +1〜3pt |
| 湿度 | 75%以上 | +1〜2pt |
| 湿度×気圧低下複合 | 75%以上かつ3h変化-4hPa以下 | +1pt |
| 降水確率 | 60%以上 | +1〜2pt |
| 気温急変3h | 5℃以上の変化 | +1〜2pt |
| 降水量 | 2mmを超える（降水スコアが2pt未満のとき） | +1pt |

### リスクレベル変換

| スコア | リスク |
|--------|--------|
| 7pt以上 | 4: 危険 🔴 |
| 4〜6pt | 3: 注意 🟠 |
| 1〜3pt | 2: やや注意 🟢 |
| 0pt | 1: 安心 🟢 |

⚠️ レベル1と2の絵文字が同一（どちらも🟢）。`src/index.ts` の `RISK` マップの実態であり、
表示上は区別が付かない。意図的でなければ修正候補。

⚠️ 本文書はレガシーのTypeScript版（LINE通知）の仕様。
iOSアプリ版のリスクエンジンは絶対気圧の評価方法が異なる（固定閾値ではなくパーセンタイル方式）。
そちらの仕様は `docs/riskengine-api.md` を参照。

## 寒暖差アラート（差別化機能）

前日比の最高気温差が5℃以上の場合に注意報を出す。
- 朝の予報メッセージに追加表示
- アラートメッセージのアドバイスにも反映
- `past_days=1` パラメータで昨日の気象データを取得して比較

## 通知ロジック

```
毎時0分に実行
    ↓
夜間（22:00〜8:30）ならスキップ
    ↓
Open-Meteo APIから気象予報を取得（昨日〜3日後）
    ↓
今後24時間の毎時複合リスクスコアを計算
    ↓
8:30〜9:30 → 朝の総合予報を送信（時間帯別リスク＋寒暖差）
それ以外 → 現在リスク<3かつ**1時間後**にリスク3以上なら緊急アラート
```

## メッセージ内容

### 朝の予報

- 今日の最大リスクレベル
- 3時間ブロック別のリスク・気圧・気温・湿度
- ピーク時間帯の警告と薬服用アドバイス
- 寒暖差注意報（5℃以上の場合）
- 具体的な対処法（湿度が高ければ除湿、気温差があれば服装調整など）

### アラート（緊急通知）

- リスクレベルとスコア
- 気圧変動方向と変化量
- 気圧推移グラフ（矢印）
- 要因内訳（気圧・湿度・降水・気温変動の各スコア）
- 症状発現予測時刻
- 状況に応じた具体的アドバイス

## Open-Meteo API パラメータ

```
hourly: pressure_msl, temperature_2m, relative_humidity_2m,
        precipitation_probability, precipitation
timezone: Asia/Tokyo
forecast_days: 3
past_days: 1
```

## 設定値

| 項目 | 値 | 説明 |
|------|-----|------|
| 通知オフ | 22:00〜8:30 | この時間帯は通知しない |
| 朝の予報 | 8:30〜10:00 | 総合予報を送信 |
| アラート条件 | リスク3以上 | **1時間後**に到達予測時に通知（現在3以上なら発火しない） |
| 寒暖差閾値 | 5℃ | 前日比最高気温差 |
| 地域 | 練馬区 | 緯度35.74, 経度139.65 |

## 技術スタック

| 項目 | 技術 |
|------|------|
| 実行環境 | GitHub Actions (cron: 毎時0分) |
| 言語 | TypeScript |
| 気象データ | Open-Meteo API（無料・キー不要）＋ 気象庁（降水確率・ナウキャスト） |
| 通知 | LINE Messaging API |

## ファイル構成

```
zutsuu/
├── .github/workflows/check.yml  # 定期実行設定
├── src/index.ts                 # メインロジック（単一ファイル構成）
├── package.json
├── tsconfig.json
├── SPEC.md                      # この仕様書
├── CLAUDE.md                    # Claude向けガイド
└── .gitignore
```

## セットアップ

### 1. LINE公式アカウント作成

1. https://developers.line.biz/ にアクセス
2. プロバイダー作成 → Messaging APIチャネル作成
3. 「Messaging API設定」でチャネルアクセストークン（長期）を発行
4. 「チャネル基本設定」であなたのユーザーID（Uから始まる）を確認
5. QRコードから友だち追加

### 2. GitHubリポジトリ作成

```bash
git init
git add .
git commit -m "Initial commit"
gh repo create zutsuu --private --source=. --push
```

### 3. Secretsにトークン登録

```bash
gh secret set LINE_CHANNEL_TOKEN
gh secret set LINE_USER_ID
```

### 4. 動作確認

```bash
npm run check                                   # テストモード（コンソール出力）
LINE_CHANNEL_TOKEN="xxx" LINE_USER_ID="Uxxx" npm run check  # LINE通知あり
gh workflow run check.yml                       # GitHub Actionsで手動実行
```

## 気象病について

### メカニズム

1. 内耳が気圧センサーとして機能
2. 気圧変化を検知すると交感神経が過剰に興奮
3. 自律神経のバランスが崩れ、症状が発現

### 影響する気象要素

- **気圧変化**：低気圧だけでなく「変化」が問題。上昇時も下降時も体調不良が起こる
- **湿度**：高湿度は気道・関節への影響を増大させる
- **気温変動**：急激な寒暖差は自律神経を乱す
- **降水**：雨天時は気圧低下と湿度上昇が重なりやすい

### 対策

- 事前に薬を服用（五苓散など）
- 耳周りのマッサージ・温め
- 高湿度時は室内除湿
- 寒暖差が大きい日は服装調整・体温管理
- 規則正しい生活、十分な睡眠
