# 気圧アラート (zutsuu)

気圧変化による体調不良を事前に通知し、薬を飲むタイミングを知らせるツール。

## 概要

- 1時間ごとに気圧予報をチェック
- 気圧の急変が始まる**約1時間前**にLINEで通知
- 上昇・下降どちらも検知（気圧の「変化」が体調に影響するため）
- 夜間（22:00〜8:30）は通知オフ

## 通知ロジック

```
毎時0分に実行
    ↓
夜間（22:00〜8:30）ならスキップ
    ↓
Open-Meteo APIから48時間分の気圧予報を取得
    ↓
今後12時間の毎時気圧変化を計算
    ↓
1時間あたり ±2hPa 以上の変化（＝急変）を探す
    ↓
急変開始の約1時間前なら通知
```

## 設定値

| 項目 | 値 | 説明 |
|------|-----|------|
| 急変閾値 | ±2 hPa/時 | これ以上の変化で「急変」と判定 |
| 通知タイミング | 1時間前 | 急変開始の何時間前に通知するか |
| 通知オフ | 22:00〜8:30 | この時間帯は通知しない |
| 地域 | 練馬区 | 緯度35.74, 経度139.65 |

## 通知メッセージ例

```
⚠️ 気圧低下アラート

📍 練馬区
🕐 14:00〜17:00
📉 1018 → 1012 hPa（-6）

💊 今が薬を飲むタイミング
```

```
⚠️ 気圧上昇アラート

📍 練馬区
🕐 18:00〜21:00
📈 1010 → 1016 hPa（+6）

💊 今が薬を飲むタイミング
```

## 技術スタック

| 項目 | 技術 |
|------|------|
| 実行環境 | GitHub Actions (cron: 毎時0分) |
| 言語 | TypeScript |
| 気圧データ | Open-Meteo API (無料・キー不要) |
| 通知 | LINE Messaging API |

## ファイル構成

```
zutsuu/
├── .github/workflows/check.yml  # 定期実行設定
├── src/index.ts                 # メインロジック
├── package.json
├── tsconfig.json
├── SPEC.md                      # この仕様書
└── .gitignore
```

## セットアップ

### 1. LINE公式アカウント作成

1. https://developers.line.biz/ にアクセス
2. プロバイダー作成 → Messaging APIチャネル作成
3. 「Messaging API設定」でチャネルアクセストークン（長期）を発行
4. 「チャネル基本設定」であなたのユーザーID（Uから始まる）を確認
5. QRコードから友だち追加

### 2. GitHubリポジトリ作成

```bash
git init
git add .
git commit -m "Initial commit"
gh repo create zutsuu --private --source=. --push
```

### 3. Secretsにトークン登録

```bash
gh secret set LINE_CHANNEL_TOKEN
# チャネルアクセストークンを貼り付け

gh secret set LINE_USER_ID
# ユーザーIDを貼り付け
```

### 4. 動作確認

```bash
gh workflow run check.yml
gh run list --workflow=check.yml
```

## 気象病について

### メカニズム

1. 内耳が気圧センサーとして機能
2. 気圧変化を検知すると交感神経が過剰に興奮
3. 自律神経のバランスが崩れ、症状が発現

### ポイント

- **低気圧だけでなく、気圧の「変化」が問題**
- 上昇時も下降時も体調不良は起こる
- 1時間あたり±2hPa以上の変化が要注意

### 対策

- 事前に薬を服用（五苓散など）
- 耳周りのマッサージ・温め
- 規則正しい生活、十分な睡眠
